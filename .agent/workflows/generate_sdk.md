---
description: Generate TypeScript SDK from OpenAPI spec
---
This workflow is a wrapper around the existing shell script. It ensures your frontend client is in sync with backend API changes.

1. Navigate to the backend scripts directory
2. Run the generation script

```bash
cd backend/scripts && ./generate-sdk.sh
```
