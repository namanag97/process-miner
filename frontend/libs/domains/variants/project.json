{
  "name": "variants",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/domains/variants/src",
  "projectType": "library",
  "tags": ["scope:domain", "type:feature"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/domains/variants/tsconfig.json --noEmit"
      }
    }
  }
}
