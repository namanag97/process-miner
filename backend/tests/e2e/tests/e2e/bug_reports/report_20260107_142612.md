# E2E API Test Report

Generated: 2026-01-07 14:26:12

## Summary

- **Total Endpoints Tested**: 61
- **Passed**: 46
- **Failed**: 15
- **Bugs Found**: 15
  - P0 (Critical): 1
  - P1 (High): 13
  - P2 (Medium): 1
  - P3 (Low): 0


## P0 - Critical (Blocking) (1 issues)

### BUG-E2E-009: Connection Error on GET /api/v1/dev/logs/stream

**Category**: Connection
**Endpoint**: `GET /api/v1/dev/logs/stream`
**Error**: Failed to connect to server

**Root Cause**: Server not responding or connection refused

**Suggested Fix**: Check if server is running and accessible

**Reproduction**:
```bash
curl \
  -X \
  GET \
  'http://localhost:8001/api/v1/dev/logs/stream'
```

**Response**:
```json
HTTPConnectionPool(host='localhost', port=8001): Read timed out.
```

---


## P1 - High (Major Issue) (13 issues)

### BUG-E2E-001: Validation Error on GET /api/v1/algorithms/recommend

**Category**: Validation
**Endpoint**: `GET /api/v1/algorithms/recommend`
**Status Code**: 422
**Error**: [{'type': 'missing', 'loc': ['query', 'dataset_id'], 'msg': 'Field required', 'input': None}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  GET \
  'http://localhost:8001/api/v1/algorithms/recommend'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": [
        "query",
        "dataset_id"
      ],
      "msg": "Field required",
      "input": null
    }
  ]
}
```

---

### BUG-E2E-002: Validation Error on POST /api/v1/analyses

**Category**: Validation
**Endpoint**: `POST /api/v1/analyses`
**Status Code**: 422
**Error**: [{'type': 'missing', 'loc': ['query', 'dataset_id'], 'msg': 'Field required', 'input': None}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/analyses' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "name": "test_string",
  "analysis_type": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": [
        "query",
        "dataset_id"
      ],
      "msg": "Field required",
      "input": null
    }
  ]
}
```

---

### BUG-E2E-003: Validation Error on POST /api/v1/auth/register

**Category**: Validation
**Endpoint**: `POST /api/v1/auth/register`
**Status Code**: 422
**Error**: [{'type': 'value_error', 'loc': ['body', 'password'], 'msg': 'Value error, Password must contain: at least one uppercase letter, at least one digit', 'input': 'test_string', 'ctx': {'error': {}}}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/auth/register' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "email": "test@example.com",
  "password": "test_string",
  "name": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "value_error",
      "loc": [
        "body",
        "password"
      ],
      "msg": "Value error, Password must contain: at least one uppercase letter, at least one digit",
      "input": "test_string",
      "ctx": {
        "error": {}
      }
    }
  ]
}
```

---

### BUG-E2E-004: Authentication Error on Public Endpoint POST /api/v1/auth/login

**Category**: Authentication
**Endpoint**: `POST /api/v1/auth/login`
**Status Code**: 401
**Error**: Invalid email or password. Please check your credentials and try again.

**Root Cause**: Public endpoint requires authentication

**Suggested Fix**: Review endpoint security configuration

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/auth/login' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "email": "test@example.com",
  "password": "test_string"
}'
```

**Response**:
```json
{
  "type": "https://api.processmining.io/errors/ERR_600",
  "title": "Authentication Failed",
  "status": 401,
  "detail": "Invalid email or password. Please check your credentials and try again.",
  "error_code": "ERR_600",
  "timestamp": "2026-01-07T08:55:42.179674",
  "instance": "http://localhost:8001/api/v1/auth/login"
}
```

---

### BUG-E2E-005: Validation Error on POST /api/v1/conformance/check

**Category**: Validation
**Endpoint**: `POST /api/v1/conformance/check`
**Status Code**: 422
**Error**: [{'type': 'string_too_short', 'loc': ['body', 'dataset_id'], 'msg': 'String should have at least 36 characters', 'input': 'test_string', 'ctx': {'min_length': 36}}, {'type': 'string_too_short', 'loc': ['body', 'model_id'], 'msg': 'String should have at least 36 characters', 'input': 'test_string', 'ctx': {'min_length': 36}}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/conformance/check' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "dataset_id": "test_string",
  "model_id": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "string_too_short",
      "loc": [
        "body",
        "dataset_id"
      ],
      "msg": "String should have at least 36 characters",
      "input": "test_string",
      "ctx": {
        "min_length": 36
      }
    },
    {
      "type": "string_too_short",
      "loc": [
        "body",
        "model_id"
      ],
      "msg": "String should have at least 36 characters",
      "input": "test_string",
      "ctx": {
        "min_length": 36
      }
    }
  ]
}
```

---

### BUG-E2E-006: Validation Error on POST /api/v1/conformance/import-model

**Category**: Validation
**Endpoint**: `POST /api/v1/conformance/import-model`
**Status Code**: 422
**Error**: [{'type': 'missing', 'loc': ['query', 'project_id'], 'msg': 'Field required', 'input': None}, {'type': 'missing', 'loc': ['query', 'model_name'], 'msg': 'Field required', 'input': None}, {'type': 'missing', 'loc': ['query', 'model_content'], 'msg': 'Field required', 'input': None}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/conformance/import-model' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": [
        "query",
        "project_id"
      ],
      "msg": "Field required",
      "input": null
    },
    {
      "type": "missing",
      "loc": [
        "query",
        "model_name"
      ],
      "msg": "Field required",
      "input": null
    },
    {
      "type": "missing",
      "loc": [
        "query",
        "model_content"
      ],
      "msg": "Field required",
      "input": null
    }
  ]
}
```

---

### BUG-E2E-007: Validation Error on POST /api/v1/datasets/

**Category**: Validation
**Endpoint**: `POST /api/v1/datasets/`
**Status Code**: 422
**Error**: [{'type': 'missing', 'loc': ['body', 'file'], 'msg': 'Field required', 'input': None}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/datasets/' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "file": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": [
        "body",
        "file"
      ],
      "msg": "Field required",
      "input": null
    }
  ]
}
```

---

### BUG-E2E-008: Validation Error on POST /api/v1/datasets/presign

**Category**: Validation
**Endpoint**: `POST /api/v1/datasets/presign`
**Status Code**: 422
**Error**: File type '' is not supported. Please upload a CSV or XES file.

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/datasets/presign' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "filename": "test_string"
}'
```

**Response**:
```json
{
  "type": "https://api.processmining.io/errors/ERR_105",
  "title": "Invalid File Type",
  "status": 422,
  "detail": "File type '' is not supported. Please upload a CSV or XES file.",
  "error_code": "ERR_105",
  "timestamp": "2026-01-07T08:55:42.537678",
  "filename": "test_string",
  "expected_types": [
    ".csv",
    ".xes"
  ],
  "instance": "http://localhost:8001/api/v1/datasets/presign"
}
```

---

### BUG-E2E-010: Validation Error on POST /api/v1/discovery/discover

**Category**: Validation
**Endpoint**: `POST /api/v1/discovery/discover`
**Status Code**: 422
**Error**: [{'type': 'string_too_short', 'loc': ['body', 'dataset_id'], 'msg': 'String should have at least 36 characters', 'input': 'test_string', 'ctx': {'min_length': 36}}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/discovery/discover' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "dataset_id": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "string_too_short",
      "loc": [
        "body",
        "dataset_id"
      ],
      "msg": "String should have at least 36 characters",
      "input": "test_string",
      "ctx": {
        "min_length": 36
      }
    }
  ]
}
```

---

### BUG-E2E-011: Validation Error on POST /api/v1/ocpm/upload

**Category**: Validation
**Endpoint**: `POST /api/v1/ocpm/upload`
**Status Code**: 422
**Error**: [{'type': 'missing', 'loc': ['body', 'file'], 'msg': 'Field required', 'input': None}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/ocpm/upload' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "file": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": [
        "body",
        "file"
      ],
      "msg": "Field required",
      "input": null
    }
  ]
}
```

---

### BUG-E2E-013: Validation Error on POST /api/v1/organizations/

**Category**: Validation
**Endpoint**: `POST /api/v1/organizations/`
**Status Code**: 422
**Error**: Organization slug 'test_string' already exists

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/organizations/' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "name": "test_string"
}'
```

**Response**:
```json
{
  "type": "https://api.processmining.io/errors/ERR_100",
  "title": "Validation Failed",
  "status": 422,
  "detail": "Organization slug 'test_string' already exists",
  "error_code": "ERR_100",
  "timestamp": "2026-01-07T08:56:12.670637",
  "instance": "http://localhost:8001/api/v1/organizations/"
}
```

---

### BUG-E2E-014: Validation Error on POST /api/v1/projects

**Category**: Validation
**Endpoint**: `POST /api/v1/projects`
**Status Code**: 422
**Error**: [{'type': 'missing', 'loc': ['query', 'workspace_id'], 'msg': 'Field required', 'input': None}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/projects' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "name": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": [
        "query",
        "workspace_id"
      ],
      "msg": "Field required",
      "input": null
    }
  ]
}
```

---

### BUG-E2E-015: Validation Error on POST /api/v1/workspaces

**Category**: Validation
**Endpoint**: `POST /api/v1/workspaces`
**Status Code**: 422
**Error**: [{'type': 'missing', 'loc': ['query', 'org_id'], 'msg': 'Field required', 'input': None}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/workspaces' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "name": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": [
        "query",
        "org_id"
      ],
      "msg": "Field required",
      "input": null
    }
  ]
}
```

---


## P2 - Medium (Minor Issue) (1 issues)

### BUG-E2E-012: Not Found Error on POST /api/v1/ocpm/discover

**Category**: Not Found
**Endpoint**: `POST /api/v1/ocpm/discover`
**Status Code**: 404
**Error**: OCEL log not found

**Root Cause**: Endpoint or resource not found

**Suggested Fix**: Check if endpoint is registered and resource exists

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/ocpm/discover' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "dataset_id": "test_string"
}'
```

**Response**:
```json
{
  "detail": "OCEL log not found"
}
```

---
