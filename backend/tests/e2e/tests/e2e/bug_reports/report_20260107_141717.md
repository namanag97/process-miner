# E2E API Test Report

Generated: 2026-01-07 14:17:17

## Summary

- **Total Endpoints Tested**: 61
- **Passed**: 43
- **Failed**: 18
- **Bugs Found**: 18
  - P0 (Critical): 6
  - P1 (High): 12
  - P2 (Medium): 0
  - P3 (Low): 0


## P0 - Critical (Blocking) (6 issues)

### BUG-E2E-005: Internal Server Error on POST /api/v1/auth/reset-password

**Category**: Internal Server Error
**Endpoint**: `POST /api/v1/auth/reset-password`
**Status Code**: 500
**Error**: Password reset token validation is not yet available. Please contact support.

**Root Cause**: Internal server error

**Suggested Fix**: Check server logs for stack trace

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/auth/reset-password' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "token": "test_string",
  "new_password": "test_string"
}'
```

**Response**:
```json
{
  "type": "https://api.processmining.io/errors/ERR_300",
  "title": "Processing Failed",
  "status": 500,
  "detail": "Password reset token validation is not yet available. Please contact support.",
  "error_code": "ERR_300",
  "timestamp": "2026-01-07T08:46:34.376095",
  "code": "NOT_IMPLEMENTED",
  "instance": "http://localhost:8001/api/v1/auth/reset-password"
}
```

---

### BUG-E2E-008: Internal Server Error on GET /api/v1/datasets/

**Category**: Internal Server Error
**Endpoint**: `GET /api/v1/datasets/`
**Status Code**: 500
**Error**: (sqlite3.OperationalError) no such column: datasets.parquet_s3_key
[SQL: SELECT count(*) AS count_1 
FROM (SELECT datasets.id AS id, datasets.name AS name, datasets.source_file AS source_file, datasets.source_format AS source_format, datasets.project_id AS project_id, datasets.total_cases AS total_cases, datasets.total_events AS total_events, datasets.total_activities AS total_activities, datasets.activities_json AS activities_json, datasets.statistics_json AS statistics_json, datasets.status AS status, datasets.error_message AS error_message, datasets.mapping_json AS mapping_json, datasets.column_suggestions_json AS column_suggestions_json, datasets.detected_columns_json AS detected_columns_json, datasets.file_size_bytes AS file_size_bytes, datasets.storage_key AS storage_key, datasets.validation_job_id AS validation_job_id, datasets.ingestion_job_id AS ingestion_job_id, datasets.source_dataset_id AS source_dataset_id, datasets.filter_config_json AS filter_config_json, datasets.is_filtered AS is_filtered, datasets.filter_stats_json AS filter_stats_json, datasets.workflow_id AS workflow_id, datasets.parquet_s3_key AS parquet_s3_key, datasets.parquet_size_bytes AS parquet_size_bytes, datasets.parquet_row_count AS parquet_row_count, datasets.created_at AS created_at, datasets.updated_at AS updated_at 
FROM datasets) AS anon_1]
(Background on this error at: https://sqlalche.me/e/20/e3q8)

**Root Cause**: Internal server error

**Suggested Fix**: Check server logs for stack trace

**Reproduction**:
```bash
curl \
  -X \
  GET \
  'http://localhost:8001/api/v1/datasets/' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing'
```

**Response**:
```json
{
  "type": "https://api.processmining.io/errors/ERR_500",
  "title": "Internal Server Error",
  "status": 500,
  "detail": "(sqlite3.OperationalError) no such column: datasets.parquet_s3_key\n[SQL: SELECT count(*) AS count_1 \nFROM (SELECT datasets.id AS id, datasets.name AS name, datasets.source_file AS source_file, datasets.source_format AS source_format, datasets.project_id AS project_id, datasets.total_cases AS total_cases, datasets.total_events AS total_events, datasets.total_activities AS total_activities, datasets.activities_json AS activities_json, datasets.statistics_json AS statistics_json, datasets.status AS status, datasets.error_message AS error_message, datasets.mapping_json AS mapping_json, datasets.column_suggestions_json AS column_suggestions_json, datasets.detected_columns_json AS detected_columns_json, datasets.file_size_bytes AS file_size_bytes, datasets.storage_key AS storage_key, datasets.validation_job_id AS validation_job_id, datasets.ingestion_job_id AS ingestion_job_id, datasets.source_dataset_id AS source_dataset_id, datasets.filter_config_json AS filter_config_json, datasets.is_filtered AS is_filtered, datasets.filter_stats_json AS filter_stats_json, datasets.workflow_id AS workflow_id, datasets.parquet_s3_key AS parquet_s3_key, datasets.parquet_size_bytes AS parquet_size_bytes, datasets.parquet_row_count AS parquet_row_count, datasets.created_at AS created_at, datasets.updated_at AS updated_at \nFROM datasets) AS anon_1]\n(Background on this error at: https://sqlalche.me/e/20/e3q8)",
  "error_code": "ERR_500",
  "instance": "http://localhost:8001/api/v1/datasets/",
  "timestamp": "2026-01-07T08:46:38.374899+00:00"
}
```

---

### BUG-E2E-011: Internal Server Error on GET /api/v1/dev/data/tables

**Category**: Internal Server Error
**Endpoint**: `GET /api/v1/dev/data/tables`
**Status Code**: 500
**Error**: No inspection system is available for object of type <class 'sqlalchemy.orm.session.Session'>

**Root Cause**: Internal server error

**Suggested Fix**: Check server logs for stack trace

**Reproduction**:
```bash
curl \
  -X \
  GET \
  'http://localhost:8001/api/v1/dev/data/tables'
```

**Response**:
```json
{
  "type": "https://api.processmining.io/errors/ERR_500",
  "title": "Internal Server Error",
  "status": 500,
  "detail": "No inspection system is available for object of type <class 'sqlalchemy.orm.session.Session'>",
  "error_code": "ERR_500",
  "instance": "http://localhost:8001/api/v1/dev/data/tables",
  "timestamp": "2026-01-07T08:46:40.517396+00:00"
}
```

---

### BUG-E2E-012: Connection Error on GET /api/v1/dev/logs/stream

**Category**: Connection
**Endpoint**: `GET /api/v1/dev/logs/stream`
**Error**: Failed to connect to server

**Root Cause**: Server not responding or connection refused

**Suggested Fix**: Check if server is running and accessible

**Reproduction**:
```bash
curl \
  -X \
  GET \
  'http://localhost:8001/api/v1/dev/logs/stream'
```

**Response**:
```json
HTTPConnectionPool(host='localhost', port=8001): Read timed out.
```

---

### BUG-E2E-015: Internal Server Error on GET /api/v1/ocpm/logs

**Category**: Internal Server Error
**Endpoint**: `GET /api/v1/ocpm/logs`
**Status Code**: 500
**Error**: (sqlite3.OperationalError) no such column: ocel_logs.ocel_storage_key
[SQL: SELECT ocel_logs.id, ocel_logs.name, ocel_logs.source_file, ocel_logs.source_format, ocel_logs.total_events, ocel_logs.total_objects, ocel_logs.total_object_types, ocel_logs.metadata_json, ocel_logs.ocel_storage_key, ocel_logs.created_at 
FROM ocel_logs ORDER BY ocel_logs.created_at DESC]
(Background on this error at: https://sqlalche.me/e/20/e3q8)

**Root Cause**: Internal server error

**Suggested Fix**: Check server logs for stack trace

**Reproduction**:
```bash
curl \
  -X \
  GET \
  'http://localhost:8001/api/v1/ocpm/logs' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing'
```

**Response**:
```json
{
  "type": "https://api.processmining.io/errors/ERR_500",
  "title": "Internal Server Error",
  "status": 500,
  "detail": "(sqlite3.OperationalError) no such column: ocel_logs.ocel_storage_key\n[SQL: SELECT ocel_logs.id, ocel_logs.name, ocel_logs.source_file, ocel_logs.source_format, ocel_logs.total_events, ocel_logs.total_objects, ocel_logs.total_object_types, ocel_logs.metadata_json, ocel_logs.ocel_storage_key, ocel_logs.created_at \nFROM ocel_logs ORDER BY ocel_logs.created_at DESC]\n(Background on this error at: https://sqlalche.me/e/20/e3q8)",
  "error_code": "ERR_500",
  "instance": "http://localhost:8001/api/v1/ocpm/logs",
  "timestamp": "2026-01-07T08:47:14.079940+00:00"
}
```

---

### BUG-E2E-016: Internal Server Error on POST /api/v1/ocpm/discover

**Category**: Internal Server Error
**Endpoint**: `POST /api/v1/ocpm/discover`
**Status Code**: 500
**Error**: (sqlite3.OperationalError) no such column: ocel_logs.ocel_storage_key
[SQL: SELECT ocel_logs.id, ocel_logs.name, ocel_logs.source_file, ocel_logs.source_format, ocel_logs.total_events, ocel_logs.total_objects, ocel_logs.total_object_types, ocel_logs.metadata_json, ocel_logs.ocel_storage_key, ocel_logs.created_at 
FROM ocel_logs 
WHERE ocel_logs.id = ?]
[parameters: ('test_string',)]
(Background on this error at: https://sqlalche.me/e/20/e3q8)

**Root Cause**: Internal server error

**Suggested Fix**: Check server logs for stack trace

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/ocpm/discover' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "dataset_id": "test_string"
}'
```

**Response**:
```json
{
  "type": "https://api.processmining.io/errors/ERR_500",
  "title": "Internal Server Error",
  "status": 500,
  "detail": "(sqlite3.OperationalError) no such column: ocel_logs.ocel_storage_key\n[SQL: SELECT ocel_logs.id, ocel_logs.name, ocel_logs.source_file, ocel_logs.source_format, ocel_logs.total_events, ocel_logs.total_objects, ocel_logs.total_object_types, ocel_logs.metadata_json, ocel_logs.ocel_storage_key, ocel_logs.created_at \nFROM ocel_logs \nWHERE ocel_logs.id = ?]\n[parameters: ('test_string',)]\n(Background on this error at: https://sqlalche.me/e/20/e3q8)",
  "error_code": "ERR_500",
  "instance": "http://localhost:8001/api/v1/ocpm/discover",
  "timestamp": "2026-01-07T08:47:17.911485+00:00"
}
```

---


## P1 - High (Major Issue) (12 issues)

### BUG-E2E-001: Validation Error on GET /api/v1/algorithms/recommend

**Category**: Validation
**Endpoint**: `GET /api/v1/algorithms/recommend`
**Status Code**: 422
**Error**: [{'type': 'missing', 'loc': ['query', 'dataset_id'], 'msg': 'Field required', 'input': None}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  GET \
  'http://localhost:8001/api/v1/algorithms/recommend'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": [
        "query",
        "dataset_id"
      ],
      "msg": "Field required",
      "input": null
    }
  ]
}
```

---

### BUG-E2E-002: Validation Error on POST /api/v1/analyses

**Category**: Validation
**Endpoint**: `POST /api/v1/analyses`
**Status Code**: 422
**Error**: [{'type': 'missing', 'loc': ['query', 'dataset_id'], 'msg': 'Field required', 'input': None}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/analyses' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "name": "test_string",
  "analysis_type": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": [
        "query",
        "dataset_id"
      ],
      "msg": "Field required",
      "input": null
    }
  ]
}
```

---

### BUG-E2E-003: Validation Error on POST /api/v1/auth/register

**Category**: Validation
**Endpoint**: `POST /api/v1/auth/register`
**Status Code**: 422
**Error**: [{'type': 'value_error', 'loc': ['body', 'password'], 'msg': 'Value error, Password must contain: at least one uppercase letter, at least one digit', 'input': 'test_string', 'ctx': {'error': {}}}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/auth/register' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "email": "test@example.com",
  "password": "test_string",
  "name": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "value_error",
      "loc": [
        "body",
        "password"
      ],
      "msg": "Value error, Password must contain: at least one uppercase letter, at least one digit",
      "input": "test_string",
      "ctx": {
        "error": {}
      }
    }
  ]
}
```

---

### BUG-E2E-004: Authentication Error on Public Endpoint POST /api/v1/auth/login

**Category**: Authentication
**Endpoint**: `POST /api/v1/auth/login`
**Status Code**: 401
**Error**: Invalid email or password. Please check your credentials and try again.

**Root Cause**: Public endpoint requires authentication

**Suggested Fix**: Review endpoint security configuration

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/auth/login' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "email": "test@example.com",
  "password": "test_string"
}'
```

**Response**:
```json
{
  "type": "https://api.processmining.io/errors/ERR_600",
  "title": "Authentication Failed",
  "status": 401,
  "detail": "Invalid email or password. Please check your credentials and try again.",
  "error_code": "ERR_600",
  "timestamp": "2026-01-07T08:46:34.313368",
  "instance": "http://localhost:8001/api/v1/auth/login"
}
```

---

### BUG-E2E-006: Validation Error on POST /api/v1/conformance/check

**Category**: Validation
**Endpoint**: `POST /api/v1/conformance/check`
**Status Code**: 422
**Error**: [{'type': 'string_too_short', 'loc': ['body', 'dataset_id'], 'msg': 'String should have at least 36 characters', 'input': 'test_string', 'ctx': {'min_length': 36}}, {'type': 'string_too_short', 'loc': ['body', 'model_id'], 'msg': 'String should have at least 36 characters', 'input': 'test_string', 'ctx': {'min_length': 36}}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/conformance/check' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "dataset_id": "test_string",
  "model_id": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "string_too_short",
      "loc": [
        "body",
        "dataset_id"
      ],
      "msg": "String should have at least 36 characters",
      "input": "test_string",
      "ctx": {
        "min_length": 36
      }
    },
    {
      "type": "string_too_short",
      "loc": [
        "body",
        "model_id"
      ],
      "msg": "String should have at least 36 characters",
      "input": "test_string",
      "ctx": {
        "min_length": 36
      }
    }
  ]
}
```

---

### BUG-E2E-007: Validation Error on POST /api/v1/conformance/import-model

**Category**: Validation
**Endpoint**: `POST /api/v1/conformance/import-model`
**Status Code**: 422
**Error**: [{'type': 'missing', 'loc': ['query', 'project_id'], 'msg': 'Field required', 'input': None}, {'type': 'missing', 'loc': ['query', 'model_name'], 'msg': 'Field required', 'input': None}, {'type': 'missing', 'loc': ['query', 'model_content'], 'msg': 'Field required', 'input': None}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/conformance/import-model' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": [
        "query",
        "project_id"
      ],
      "msg": "Field required",
      "input": null
    },
    {
      "type": "missing",
      "loc": [
        "query",
        "model_name"
      ],
      "msg": "Field required",
      "input": null
    },
    {
      "type": "missing",
      "loc": [
        "query",
        "model_content"
      ],
      "msg": "Field required",
      "input": null
    }
  ]
}
```

---

### BUG-E2E-009: Validation Error on POST /api/v1/datasets/

**Category**: Validation
**Endpoint**: `POST /api/v1/datasets/`
**Status Code**: 422
**Error**: [{'type': 'missing', 'loc': ['body', 'file'], 'msg': 'Field required', 'input': None}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/datasets/' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "file": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": [
        "body",
        "file"
      ],
      "msg": "Field required",
      "input": null
    }
  ]
}
```

---

### BUG-E2E-010: Validation Error on POST /api/v1/datasets/presign

**Category**: Validation
**Endpoint**: `POST /api/v1/datasets/presign`
**Status Code**: 422
**Error**: File type '' is not supported. Please upload a CSV or XES file.

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/datasets/presign' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "filename": "test_string"
}'
```

**Response**:
```json
{
  "type": "https://api.processmining.io/errors/ERR_105",
  "title": "Invalid File Type",
  "status": 422,
  "detail": "File type '' is not supported. Please upload a CSV or XES file.",
  "error_code": "ERR_105",
  "timestamp": "2026-01-07T08:46:38.481255",
  "filename": "test_string",
  "expected_types": [
    ".xes",
    ".csv"
  ],
  "instance": "http://localhost:8001/api/v1/datasets/presign"
}
```

---

### BUG-E2E-013: Validation Error on POST /api/v1/discovery/discover

**Category**: Validation
**Endpoint**: `POST /api/v1/discovery/discover`
**Status Code**: 422
**Error**: [{'type': 'string_too_short', 'loc': ['body', 'dataset_id'], 'msg': 'String should have at least 36 characters', 'input': 'test_string', 'ctx': {'min_length': 36}}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/discovery/discover' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "dataset_id": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "string_too_short",
      "loc": [
        "body",
        "dataset_id"
      ],
      "msg": "String should have at least 36 characters",
      "input": "test_string",
      "ctx": {
        "min_length": 36
      }
    }
  ]
}
```

---

### BUG-E2E-014: Validation Error on POST /api/v1/ocpm/upload

**Category**: Validation
**Endpoint**: `POST /api/v1/ocpm/upload`
**Status Code**: 422
**Error**: [{'type': 'missing', 'loc': ['body', 'file'], 'msg': 'Field required', 'input': None}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/ocpm/upload' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "file": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": [
        "body",
        "file"
      ],
      "msg": "Field required",
      "input": null
    }
  ]
}
```

---

### BUG-E2E-017: Validation Error on POST /api/v1/projects

**Category**: Validation
**Endpoint**: `POST /api/v1/projects`
**Status Code**: 422
**Error**: [{'type': 'missing', 'loc': ['query', 'workspace_id'], 'msg': 'Field required', 'input': None}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/projects' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "name": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": [
        "query",
        "workspace_id"
      ],
      "msg": "Field required",
      "input": null
    }
  ]
}
```

---

### BUG-E2E-018: Validation Error on POST /api/v1/workspaces

**Category**: Validation
**Endpoint**: `POST /api/v1/workspaces`
**Status Code**: 422
**Error**: [{'type': 'missing', 'loc': ['query', 'org_id'], 'msg': 'Field required', 'input': None}]

**Root Cause**: Request validation failed

**Suggested Fix**: Check request schema and required fields

**Reproduction**:
```bash
curl \
  -X \
  POST \
  'http://localhost:8001/api/v1/workspaces' \
  -H \
  'Authorization: Bearer test-token-for-e2e-testing' \
  -H \
  'Content-Type: application/json' \
  -d \
  '{
  "name": "test_string"
}'
```

**Response**:
```json
{
  "detail": [
    {
      "type": "missing",
      "loc": [
        "query",
        "org_id"
      ],
      "msg": "Field required",
      "input": null
    }
  ]
}
```

---
